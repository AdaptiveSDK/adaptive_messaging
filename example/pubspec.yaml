name: adaptive_messaging_example
description: Demonstrates usage of the adaptive_messaging_flutter plugin.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  adaptive_core_flutter:
    path: ../../adaptive_core_flutter
  adaptive_messaging_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
