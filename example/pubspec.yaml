name: adaptive_messaging_example
description: Demonstrates usage of the adaptive_messaging plugin.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  adaptive_core:
    path: ../../adaptive_core
  adaptive_messaging:
    path: ../
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
